# 3Dボクセル・ダンジョンゲーム 開発計画

## ユーザープロンプトの要約
GPU非搭載のPCでも軽量に動作する「3Dボクセル・ダンジョンゲーム」のベースシステムを Three.js で作成する。
- 3次元座標 `createBlock(x, y, z, type)` を用いたマップ生成。
- 複数のCube（床・壁）を効率的に描画（`MeshBasicMaterial` 等を使用）。
- 青いCubeのプレイヤー（サイズ0.8）と、背後からのTPS視点（追従カメラ）。
- 自作の物理・当たり判定（重力、ジャンプ、壁とのAABB衝突判定）。
- WASD/矢印キーによる、カメラ向き基準の移動。
- 鬼（エネミー）の実装と、Raycasterによる視界判定を用いたAIロジック（徘徊・突撃）、ゲームオーバー判定の追加。
- 固定の狭いマップから、ランダムに生成される広いマップに変更する。

## タスク

### フェーズ1: ベースシステムの構築
- [x] プロジェクトの初期化（Git, 計画作成）
- [x] `index.html` の作成（Three.js CDN, CSS）
- [x] `main.js` の作成
    - [x] Three.js の基本セットアップ（Scene, Camera, Renderer）
    - [x] マップ生成（2次元配列から `InstancedMesh` または効率的な `MeshBasicMaterial` で生成）
    - [x] プレイヤーの作成とカメラ追従
    - [x] キーボード入力の管理
    - [x] 物理演算（重力、ジャンプ）とAABB衝突判定の実装
- [x] 動作確認とデバッグ
    - [x] `ReferenceError: THREE is not defined` の修正
    - [x] 移動方向（前後左右）の反転を修正
    - [x] 壁に乗ったあとマップ外へ出られないように制限を追加
    - [x] プレイヤーの向きがわかるように顔（目）を追加し、移動方向を向かせる

### フェーズ2: エネミー（鬼）とAIの実装
- [x] 鬼（赤いCube）の生成と初期配置
- [x] THREE.Raycaster を用いた視界判定の実装
- [x] AIロジックの実装
    - [x] 徘徊モード（ランダム移動、速度0.02）
    - [x] 突撃モード（プレイヤー追従、速度0.06、色の点滅演出）
- [x] ゲームオーバー判定の実装（距離が0.8未満でアラート表示とリセット）

### フェーズ3: ランダムマップ生成
- [x] BSP（2分木空間分割）アルゴリズムを用いた部屋と通路の自動生成
- [x] マップサイズを広げる（25x25等）
- [x] プレイヤーとエネミーの初期位置をランダムな床の上に配置する
- [x] 壁が多すぎたり、初期位置が壁に埋もれないように改善

- [ ] Gitへのコミット
